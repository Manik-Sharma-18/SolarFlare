---
phase: 08-loss-function-overhaul
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - training/losses.py
  - tests/test_losses.py
autonomous: true
requirements: [LOSS-01, LOSS-02, LOSS-03, LOSS-04, LOSS-05]

must_haves:
  truths:
    - "WeightedMAELoss uses absolute threshold binary weighting, not per-sample relative normalization"
    - "AsymmetricExtremeLoss penalizes underestimation 2x above threshold, symmetric below"
    - "Temporal difference loss produces non-zero value when frame-to-frame changes differ between pred and target"
    - "Temporal variation penalty is negative (rewards variation), capped at target variation level"
    - "Per-timestep weighting makes later timesteps contribute more to loss"
  artifacts:
    - path: "training/losses.py"
      provides: "Fixed WeightedMAELoss, new AsymmetricExtremeLoss, temporal helper functions"
      contains: "AsymmetricExtremeLoss"
    - path: "tests/test_losses.py"
      provides: "Unit tests for all new/modified loss components"
      contains: "TestAsymmetricExtremeLoss"
  key_links:
    - from: "tests/test_losses.py"
      to: "training/losses.py"
      via: "import and instantiation"
      pattern: "from training.losses import.*AsymmetricExtremeLoss"
---

<objective>
TDD implementation of all new and modified loss components: fix WeightedMAELoss to use absolute threshold binary weighting (LOSS-04), create AsymmetricExtremeLoss class (LOSS-05), and implement temporal loss helper functions for temporal difference (LOSS-01), temporal variation penalty (LOSS-03), and per-timestep weighting (LOSS-02).

Purpose: These are the foundational loss building blocks that CompositeLoss.forward() will integrate in Plan 08-02. Tests-first ensures each component is mathematically correct before integration.

Output: Updated training/losses.py with fixed/new classes and helper functions; updated tests/test_losses.py with comprehensive unit tests.
</objective>

<execution_context>
@/Users/manik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/manik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-loss-function-overhaul/08-CONTEXT.md
@.planning/phases/08-loss-function-overhaul/08-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing code. Executor should use directly. -->

From training/losses.py (current WeightedMAELoss to be REPLACED):
```python
class WeightedMAELoss(nn.Module):
    def __init__(self, base_weight: float = 1.0, extreme_weight: float = 2.0):
        # BUG: forward() uses per-sample relative normalization (lines 283-284)
        # Replace with absolute threshold binary weighting

    def forward(self, pred: torch.Tensor, target: torch.Tensor) -> torch.Tensor:
        # CURRENT (buggy): normalized_magnitude = abs_target / (abs_target.max() + 1e-6)
        # NEW: extreme_mask = (target.abs() > self.threshold).float()
        #      weights = base_weight + (extreme_weight - base_weight) * extreme_mask
```

From tests/test_losses.py (existing test classes):
```python
class TestSSIM:          # 4 tests - DO NOT MODIFY
class TestMSSSIM:        # 2 tests - DO NOT MODIFY
class TestGaussianKernel: # 3 tests - DO NOT MODIFY
class TestWeightedMAE:   # 2 tests - UPDATE for new threshold behavior
class TestCompositeLoss:  # 4 tests - DO NOT MODIFY (updated in Plan 02)
class TestGetLossFunction: # 4 tests - DO NOT MODIFY (updated in Plan 02)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - Write failing tests for all loss components</name>
  <files>tests/test_losses.py</files>
  <behavior>
    - TestWeightedMAE::test_weighted_mae_absolute_threshold: Same target value at different batch max values should produce identical loss (binary weighting, not relative)
    - TestWeightedMAE::test_weighted_mae_threshold_boundary: Values exactly at threshold boundary produce correct weights (base_weight below, extreme_weight above)
    - TestAsymmetricExtremeLoss::test_asymmetric_underestimation_above_threshold: Loss for underestimating extreme regions is alpha times higher than overestimating same magnitude
    - TestAsymmetricExtremeLoss::test_symmetric_below_threshold: Below threshold, underestimation and overestimation produce equal loss
    - TestAsymmetricExtremeLoss::test_zero_error: pred == target produces loss ~0
    - TestTemporalDiffLoss::test_nonzero_for_different_dynamics: When pred has different frame-to-frame changes than target, loss is > 0
    - TestTemporalDiffLoss::test_zero_for_identical_dynamics: When pred and target have identical frame-to-frame changes (even if absolute values differ by constant offset), loss is ~0
    - TestTemporalDiffLoss::test_3d_input_raises_or_handles: Function handles 5D input correctly
    - TestTemporalVarPenalty::test_negative_value: Result is negative (rewards variation)
    - TestTemporalVarPenalty::test_capped_at_target_variation: When pred variation exceeds target variation, penalty is capped at -lambda * target_var
    - TestTemporalVarPenalty::test_zero_for_single_frame: T=1 input (no diffs possible) returns 0
    - TestTemporalWeighting::test_later_timesteps_weighted_more: With weights [1.0, 1.5, 2.0, 2.5], error at t=4 contributes 2.5x more than t=1
    - TestTemporalWeighting::test_broadcast_shape: Weights broadcast correctly to (1,1,T,1,1) shape
  </behavior>
  <action>
    Add new test classes and update existing TestWeightedMAE in tests/test_losses.py.

    Import the new components at the top of the file:
    ```python
    from training.losses import (
        ssim, ms_ssim, gaussian_kernel, _KERNEL_CACHE,
        WeightedMAELoss, CompositeLoss, get_loss_function,
        AsymmetricExtremeLoss,  # NEW
        compute_temporal_diff_loss,  # NEW
        compute_temporal_var_penalty,  # NEW
        apply_temporal_weights,  # NEW
    )
    ```

    For TestWeightedMAE updates:
    - Keep existing test_weighted_mae_zero_error (still valid)
    - Update test_weighted_mae_extreme_higher_weight to use threshold-based assertions
    - Add test_weighted_mae_absolute_threshold: Create two batches with same target values but different max values. With new absolute threshold, loss should be identical. Construct target with some values above 0.3456 and some below. threshold=0.3456.
    - Add test_weighted_mae_threshold_boundary: Create target where some values are just below threshold (0.34) and some just above (0.35). Verify weights are base_weight for below and extreme_weight for above.

    For TestAsymmetricExtremeLoss:
    - test_asymmetric_underestimation_above_threshold: Set target=0.5 (above 0.3456), pred_under=0.3 (under by 0.2), pred_over=0.7 (over by 0.2). Loss for under should be alpha=2.0 times the loss for over (in the extreme region).
    - test_symmetric_below_threshold: Set target=0.1 (below 0.3456), pred_under=0.0 (under by 0.1), pred_over=0.2 (over by 0.1). Losses should be equal.
    - test_zero_error: pred == target, loss should be ~0.

    For TestTemporalDiffLoss:
    - test_nonzero_for_different_dynamics: pred changes linearly, target changes quadratically, loss > 0. Shape (1,1,4,8,8).
    - test_zero_for_identical_dynamics: pred = target + constant_offset. Frame-to-frame diffs are identical, loss ~0.
    - test_3d_input_handles: 5D tensor (B,C,T,H,W) works. Also test T=2 (minimum for 1 diff).

    For TestTemporalVarPenalty:
    - test_negative_value: Both pred and target have frame variation. Result should be negative.
    - test_capped_at_target_variation: pred has 10x more variation than target. Result magnitude should equal lambda * target_var, NOT lambda * pred_var.
    - test_zero_for_single_frame: T=1 tensor, diffs would be empty. Return 0.0.

    For TestTemporalWeighting:
    - test_later_timesteps_weighted_more: Create a 5D loss tensor that is 1.0 everywhere. Apply weights [1.0, 1.5, 2.0, 2.5]. The weighted mean should be (1.0+1.5+2.0+2.5)/4 = 1.75, not 1.0.
    - test_broadcast_shape: Verify the function accepts (B,C,T,H,W) and returns a scalar.

    All tests should FAIL because the new classes/functions don't exist yet. Commit: test(08-01): add failing tests for loss components
  </action>
  <verify>
    <automated>cd /Users/manik/Solar/SolarFlare && python -m pytest tests/test_losses.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>New test file has 13+ new test cases. Tests fail with ImportError or AttributeError because new classes/functions don't exist yet. Existing tests for SSIM/GaussianKernel/CompositeLoss/GetLossFunction are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - Implement all loss components to pass tests</name>
  <files>training/losses.py</files>
  <action>
    Implement the following in training/losses.py, below the existing CompositeLoss class and above get_loss_function:

    1. **Fix WeightedMAELoss** (LOSS-04):
       - Add `threshold` parameter to __init__ (default 0.3456)
       - Replace forward() body: use `extreme_mask = (target.abs() > self.threshold).float()` for binary weighting
       - weights = base_weight + (extreme_weight - base_weight) * extreme_mask
       - Per user decision: base_weight=1.0, extreme_weight=3.0, no magnitude scaling within zones
       - Update docstring to explain absolute threshold behavior

    2. **Create AsymmetricExtremeLoss** (LOSS-05):
       - New nn.Module class with alpha=2.0, threshold=0.3456
       - forward(pred, target):
         - above_threshold = (target.abs() > self.threshold).float()
         - underestimation = (target - pred).clamp(min=0)
         - overestimation = (pred - target).clamp(min=0)
         - error = above_threshold * (alpha * underestimation + overestimation) + (1 - above_threshold) * (underestimation + overestimation)
         - return error.mean()
       - Per user decision: asymmetric penalty ONLY above threshold, symmetric below

    3. **Create compute_temporal_diff_loss** function (LOSS-01):
       - Takes pred (B,C,T,H,W) and target (B,C,T,H,W)
       - pred_diffs = pred[:,:,1:,:,:] - pred[:,:,:-1,:,:]
       - target_diffs = target[:,:,1:,:,:] - target[:,:,:-1,:,:]
       - return F.l1_loss(pred_diffs, target_diffs)
       - Handle T=1 edge case (return 0.0 tensor)

    4. **Create compute_temporal_var_penalty** function (LOSS-03):
       - Takes pred (B,C,T,H,W), target (B,C,T,H,W), lambda_val=0.1
       - pred_diffs/target_diffs same as above
       - pred_var = pred_diffs.abs().mean(), target_var = target_diffs.abs().mean()
       - capped_var = torch.min(pred_var, target_var)
       - return -lambda_val * capped_var
       - Handle T=1 edge case (return 0.0 tensor)
       - Per user decision: applies to ALL pixels, capped at target variation

    5. **Create apply_temporal_weights** function (LOSS-02):
       - Takes loss_tensor (B,C,T,H,W), weights list
       - tw = torch.tensor(weights[:T], device=..., dtype=...).view(1,1,T,1,1)
       - return (loss_tensor * tw).mean()
       - Per user decision: weights [1.0, 1.5, 2.0, 2.5]

    All functions should use standard PyTorch ops only. Include type hints and Google-style docstrings per project conventions.

    Commit: feat(08-01): implement loss components (WeightedMAE fix, asymmetric, temporal)
  </action>
  <verify>
    <automated>cd /Users/manik/Solar/SolarFlare && python -m pytest tests/test_losses.py -x -q</automated>
  </verify>
  <done>All tests in test_losses.py pass (existing + new). WeightedMAELoss uses absolute threshold. AsymmetricExtremeLoss exists and penalizes underestimation alpha times more above threshold. Temporal diff, var, and weighting functions work correctly. Full test suite passes with no regressions.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_losses.py -x -q` -- all loss tests pass
- `python -m pytest tests/ -x -q` -- full suite passes (no regressions)
- `python -c "from training.losses import AsymmetricExtremeLoss, compute_temporal_diff_loss, compute_temporal_var_penalty, apply_temporal_weights; print('imports OK')"` -- all new exports importable
</verification>

<success_criteria>
- WeightedMAELoss produces identical loss for same target values regardless of batch max (absolute threshold, not relative)
- AsymmetricExtremeLoss penalizes underestimation 2x more than overestimation above threshold=0.3456
- compute_temporal_diff_loss returns non-zero for different dynamics, ~0 for identical dynamics
- compute_temporal_var_penalty returns negative value capped at target variation
- apply_temporal_weights makes later timesteps contribute proportionally more
- All 124+ existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-loss-function-overhaul/08-01-SUMMARY.md`
</output>
