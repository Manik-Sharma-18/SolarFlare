---
phase: 05-mps-compatibility
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - config.yaml
  - utils/config_validator.py
  - main.py
autonomous: true

must_haves:
  truths:
    - "config.yaml has ssim_tiling_threshold field under loss section"
    - "config.yaml uncertainty.n_samples is validated as positive integer"
    - "MPS startup log line mentions alternative ops are active"
    - "Config validator rejects invalid ssim_tiling_threshold and uncertainty.n_samples"
  artifacts:
    - path: "config.yaml"
      provides: "SSIM tiling threshold and MC dropout config fields"
      contains: "ssim_tiling_threshold"
    - path: "utils/config_validator.py"
      provides: "Validation for new Phase 5 config fields"
      contains: "ssim_tiling_threshold"
  key_links:
    - from: "main.py"
      to: "utils/mps_ops.py"
      via: "MPS ops startup log at device resolution time"
      pattern: "mps_ops"
---

<objective>
Wire Phase 5 features (SSIM tiling, MPS ops logging) into config.yaml, config validator, and main.py startup.

Purpose: New config fields need defaults, documentation, and validation. MPS alternative ops should log at startup so users know the fallback is active.

Output: Updated config.yaml with new fields, config_validator.py with validation rules, main.py with MPS startup log.
</objective>

<execution_context>
@/Users/manik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/manik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mps-compatibility/05-CONTEXT.md
@.planning/phases/05-mps-compatibility/05-01-SUMMARY.md
@.planning/phases/05-mps-compatibility/05-02-SUMMARY.md
@config.yaml
@utils/config_validator.py
@main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 5 config fields and validation</name>
  <files>config.yaml, utils/config_validator.py</files>
  <action>
1. **config.yaml updates:**
   - Under `loss:` section, add:
     ```yaml
     ssim_tiling_threshold: 256  # Tile SSIM computation for spatial dims above this (prevents OOM on 512x512+)
     ```
   - Under `uncertainty:` section, verify `n_samples: 20` exists (it does). No changes needed to uncertainty section — it already has the right fields.

2. **config_validator.py updates:**
   - Add validation for `loss.ssim_tiling_threshold`:
     - Must be a positive integer if present
     - Must be >= 32 (smaller makes no sense for SSIM with window_size=11)
     - Default: 256
   - Add validation for `uncertainty.n_samples`:
     - Must be a positive integer >= 2 if uncertainty.enabled is true
     - Default: 20
   - Add validation that `uncertainty.n_samples` is reasonable (warn if > 100 — very slow)
   - Add these to the existing validation function flow, following the accumulate-all-errors pattern (ConfigValidationError).
  </action>
  <verify>
Run `python -c "
from utils.config_validator import validate_config
import yaml

# Load actual config
with open('config.yaml') as f:
    config = yaml.safe_load(f)

# Should pass without errors
try:
    validate_config(config)
    print('Config validation passed')
except Exception as e:
    print(f'Validation failed: {e}')
"` — should print "Config validation passed".
  </verify>
  <done>config.yaml has ssim_tiling_threshold field. Config validator checks ssim_tiling_threshold and uncertainty.n_samples with clear error messages.</done>
</task>

<task type="auto">
  <name>Task 2: Wire MPS ops startup log and config threading into main.py</name>
  <files>main.py</files>
  <action>
1. **MPS ops startup log:**
   - After `resolve_device()` is called in main.py, check if device is MPS.
   - If MPS: call a function from `utils/mps_ops.py` that logs the one-time INFO message about MPS alternative ops being active. This can be a simple `log_mps_ops_active()` function or just import and call `is_mps` + print.
   - The log should appear once: "MPS device detected: using alternative ops for grouped conv2d, torch.outer, torch.quantile"

2. **Thread ssim_tiling_threshold into loss config:**
   - Where the loss function is created (in main.py or trainer.py — check which builds the loss_config dict), ensure `ssim_tiling_threshold` from config is included in the loss_config dict passed to `get_loss_function()`.
   - The loss config in config.yaml already has the field from Task 1. Verify main.py passes the full `loss` config section to `get_loss_function()` — if it does, no threading needed (it flows automatically).

3. **Thread uncertainty.n_samples:**
   - Verify that the uncertainty config section is accessible where `predict_with_uncertainty` would be called. Since uncertainty estimation happens at inference time (not training), this is likely already handled by config.yaml being loaded. No explicit threading needed unless main.py calls uncertainty functions.
  </action>
  <verify>
Run `python -c "
import yaml
with open('config.yaml') as f:
    config = yaml.safe_load(f)
assert 'ssim_tiling_threshold' in config.get('loss', {}), 'ssim_tiling_threshold missing from loss config'
print(f'ssim_tiling_threshold = {config[\"loss\"][\"ssim_tiling_threshold\"]}')
print('Config threading OK')
"` — should show threshold value and print OK.
  </verify>
  <done>MPS startup log appears when device is MPS. ssim_tiling_threshold flows from config through to CompositeLoss. Config fields documented and validated.</done>
</task>

</tasks>

<verification>
1. `python -c "import yaml; c = yaml.safe_load(open('config.yaml')); assert 'ssim_tiling_threshold' in c['loss']; print('config OK')"` succeeds
2. `python -c "from utils.config_validator import validate_config; import yaml; validate_config(yaml.safe_load(open('config.yaml'))); print('validation OK')"` succeeds
3. Grep config.yaml for "ssim_tiling_threshold" — should find 1 match
4. Grep utils/config_validator.py for "ssim_tiling_threshold" — should find at least 1 match
</verification>

<success_criteria>
- config.yaml has ssim_tiling_threshold under loss section with default 256
- Config validator validates ssim_tiling_threshold (positive int >= 32) and uncertainty.n_samples (positive int >= 2)
- MPS startup message logs when device is MPS
- All existing config validation still passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-mps-compatibility/05-03-SUMMARY.md`
</output>
