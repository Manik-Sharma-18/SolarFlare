---
phase: 11-integration-testing-validation
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - generate_comparison.py
  - COMPARISON.md
  - comparison_metrics.png
  - comparison_temporal.png
  - comparison_samples.png
autonomous: false
requirements: []

must_haves:
  truths:
    - "A comparison report exists documenting v3.0 vs v2.0 on all evaluation metrics"
    - "The report shows per-timestep breakdown for MAE, RMSE, correlation, CSI with delta columns"
    - "The report includes a PASS/MIXED/REGRESSION verdict based on temporal variation ratio and CSI"
    - "Bar charts and line plots visually compare per-timestep metrics side by side"
    - "Sample prediction visualizations show qualitative temporal dynamics differences"
    - "Tradeoffs between metrics are documented neutrally"
  artifacts:
    - path: "generate_comparison.py"
      provides: "Standalone comparison report generator"
      min_lines: 150
    - path: "COMPARISON.md"
      provides: "Full diagnostic comparison report with verdict"
      contains: "Verdict"
    - path: "comparison_metrics.png"
      provides: "Per-timestep metric bar charts (MAE, RMSE, correlation)"
    - path: "comparison_temporal.png"
      provides: "Temporal dynamics comparison (variation ratio, CSI, HSS)"
    - path: "comparison_samples.png"
      provides: "Sample prediction grids showing temporal dynamics"
  key_links:
    - from: "generate_comparison.py"
      to: "outputs/test_results.json"
      via: "json.load to read v3.0 results"
      pattern: "json\\.load.*test_results"
    - from: "generate_comparison.py"
      to: "COMPARISON.md"
      via: "write markdown report with embedded PNG references"
      pattern: "COMPARISON\\.md"
    - from: "COMPARISON.md"
      to: "comparison_metrics.png"
      via: "markdown image reference"
      pattern: "comparison_metrics\\.png"
---

<objective>
Create a standalone comparison script that reads v3.0 training results and generates a comprehensive COMPARISON.md report with charts, tables, and verdict documenting v3.0 vs v2.0 performance.

Purpose: Produce the final deliverable of the v3.0 milestone -- a diagnostic comparison report that honestly documents what improved, what regressed, and the tradeoffs between temporal dynamics fidelity and per-pixel accuracy.

Output: generate_comparison.py (reusable script), COMPARISON.md (report), comparison_*.png (charts)
</objective>

<execution_context>
@/Users/indra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/indra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-integration-testing-validation/11-CONTEXT.md
@.planning/phases/11-integration-testing-validation/11-RESEARCH.md
@.planning/phases/11-integration-testing-validation/11-01-SUMMARY.md
@main.py
@outputs/test_results.json

<interfaces>
<!-- v3.0 test_results.json structure (from main.py lines 253-276): -->
Keys available in outputs/test_results.json:
- test_loss: float
- test_mae_per_timestep: list[float] (4 values, one per output frame)
- test_rmse_per_timestep: list[float] (4 values)
- test_correlation_per_timestep: list[float] (4 values)
- test_csi: float
- test_csi_per_timestep: list[float] (4 values)
- test_hss: float
- test_hss_per_timestep: list[float] (4 values)
- test_ssim: float
- test_ssim_per_timestep: list[float] (4 values)
- persistence_mae_per_timestep: list[float] (4 values)
- persistence_skill_per_timestep: list[float] (4 values)
- persistence_csi: float
- persistence_hss: float
- peak_flux_error_per_timestep: list[float] (4 values)
- temporal_variation_ratio: float

<!-- v2.0 baseline values (hardcoded per user decision): -->
V2_BASELINE = {
    "test_mae_per_timestep": [0.102, 0.109, 0.112, 0.114],
    "test_rmse_per_timestep": [0.145, 0.153, 0.156, 0.157],
    "test_correlation_per_timestep": [0.565, 0.508, 0.483, 0.467],
    "persistence_skill_per_timestep": [2.9, 4.7, 5.2, 5.1],
    "test_csi": 0.051,
    "test_hss": 0.092,
    "temporal_variation_ratio": 0.060,
    "pred_variation": 0.006,
    "target_variation": 0.105,
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comparison report generator</name>
  <files>generate_comparison.py</files>
  <action>
Create `generate_comparison.py` at project root -- a standalone script that:

1. **Hardcodes v2.0 baseline** as a Python dict (per user decision). Use the exact values from CONTEXT.md:
   - MAE per timestep: [0.102, 0.109, 0.112, 0.114]
   - RMSE per timestep: [0.145, 0.153, 0.156, 0.157]
   - Correlation per timestep: [0.565, 0.508, 0.483, 0.467]
   - Persistence skill per timestep: [2.9, 4.7, 5.2, 5.1]
   - CSI: 0.051, HSS: 0.092
   - Temporal variation ratio: 0.060
   - pred_variation: 0.006, target_variation: 0.105

2. **Loads v3.0 results** from `outputs/test_results.json` (json.load). Handle the key names as they appear in main.py (test_mae_per_timestep, test_csi, temporal_variation_ratio, etc.).

3. **Computes verdict** using this logic:
   - PASS: temporal_variation_ratio AND test_csi both improved over v2.0
   - MIXED: one improved, other same or regressed
   - REGRESSION: both key metrics same or worse
   Per user decision, no fixed minimum thresholds -- "any improvement" counts.

4. **Generates 3 chart PNGs** using matplotlib (already in requirements.txt):

   a. `comparison_metrics.png` -- 2x2 subplot grid with grouped bar charts:
      - Per-timestep MAE (v2.0 vs v3.0)
      - Per-timestep RMSE (v2.0 vs v3.0)
      - Per-timestep Correlation (v2.0 vs v3.0)
      - Per-timestep Persistence Skill (v2.0 vs v3.0)
      Each subplot has timestep labels (t+1, t+2, t+3, t+4), value labels on bars, legend.

   b. `comparison_temporal.png` -- 1x3 subplot row with scalar comparisons:
      - Temporal Variation Ratio (v2.0 bar vs v3.0 bar, with target_variation reference line)
      - CSI (v2.0 vs v3.0)
      - HSS (v2.0 vs v3.0)
      Clear labels, value annotations on bars.

   c. `comparison_samples.png` -- Load outputs/predictions.png (v3.0 predictions already generated by main.py) and display alongside a note that v2.0 predictions are visually near-identical frames (persistence behavior with var_ratio 0.060). If outputs/predictions.png does not exist, skip this chart with a warning.

   Chart styling (Claude's discretion): Use a clean professional style (matplotlib default with grid), consistent color scheme (blue for v2.0, orange for v3.0), 150 DPI, tight layout.

5. **Generates COMPARISON.md** at project root with this structure:

   ```
   # SolarFlare v3.0 vs v2.0 Comparison Report

   **Date:** {generated timestamp}
   **Verdict:** {PASS|MIXED|REGRESSION}

   ## Summary
   {1-2 paragraph prose summarizing key findings}

   ## Key Metrics
   | Metric | v2.0 | v3.0 | Delta | % Change |
   |--------|------|------|-------|----------|
   (temporal variation ratio, CSI, HSS, SSIM, avg persistence skill)

   ## Per-Timestep Metrics
   ### MAE
   | Timestep | v2.0 | v3.0 | Delta | % Change |
   (repeat for RMSE, Correlation, Persistence Skill, CSI per timestep)

   ## Temporal Dynamics Analysis
   (prose: var ratio, pred_variation vs target_variation)

   ## Flare Detection Analysis
   (prose: CSI/HSS changes)

   ## Tradeoffs
   (neutral MAE/RMSE vs temporal metrics discussion)

   ## Visualizations
   (embedded PNGs)

   ## Configuration
   (key v3.0 config values)

   ## Methodology
   (single run, seed 42, 50 epochs, MPS)
   ```

6. Script should be runnable standalone: `python generate_comparison.py` with optional `--output-dir` argument (default: project root).

Important: Do NOT read from "outputs copy/" -- v2.0 values are hardcoded. Only read from "outputs/" for v3.0 results. Do NOT modify any existing model, training, or test code.
  </action>
  <verify>
    <automated>python generate_comparison.py && test -f COMPARISON.md && test -f comparison_metrics.png && test -f comparison_temporal.png && echo "Report and charts generated successfully"</automated>
  </verify>
  <done>generate_comparison.py exists and runs successfully. COMPARISON.md generated at project root with verdict, metric tables with delta columns, and prose analysis. comparison_metrics.png and comparison_temporal.png generated with side-by-side charts. All v2.0 baselines hardcoded, all v3.0 values loaded from outputs/test_results.json.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify comparison report</name>
  <files>COMPARISON.md, comparison_metrics.png, comparison_temporal.png</files>
  <action>Human verifies the comparison report is complete, accurate, and honestly represents the v3.0 vs v2.0 results. Steps: (1) Open COMPARISON.md and check verdict matches metrics. (2) Review metric tables for correct deltas. (3) Open comparison_metrics.png and comparison_temporal.png for chart clarity. (4) Read Tradeoffs section for neutral tone. (5) Confirm nothing is missing.</action>
  <verify>
    <automated>test -f COMPARISON.md && test -f comparison_metrics.png && test -f comparison_temporal.png && echo "All report artifacts present"</automated>
  </verify>
  <done>User reviewed COMPARISON.md, confirmed verdict accuracy, metric tables correctness, chart clarity, and neutral tradeoff documentation. Report approved as final v3.0 milestone deliverable.</done>
  <what-built>Complete v3.0 vs v2.0 comparison report (COMPARISON.md) with metric tables, charts, and automated verdict. This is the final deliverable of the v3.0 milestone.</what-built>
  <how-to-verify>
    1. Open COMPARISON.md and read the verdict (PASS/MIXED/REGRESSION) -- does it match the metrics?
    2. Review metric tables -- are delta columns correct? Do percentage changes make sense?
    3. Open comparison_metrics.png -- do the bar charts clearly show per-timestep differences?
    4. Open comparison_temporal.png -- are CSI, HSS, and variation ratio visualized clearly?
    5. Read the Tradeoffs section -- are MAE/RMSE regressions (if any) documented neutrally?
    6. Does the report honestly represent the results without overstating improvements?
    7. Is there anything missing from the report that should be added?
  </how-to-verify>
  <resume-signal>Type "approved" if the comparison report is satisfactory, or describe changes needed.</resume-signal>
</task>

</tasks>

<verification>
- generate_comparison.py runs without errors
- COMPARISON.md exists at project root with all required sections
- Verdict correctly classifies results as PASS/MIXED/REGRESSION
- All per-timestep metrics have delta and % change columns
- 3 PNG chart files generated (comparison_metrics.png, comparison_temporal.png, comparison_samples.png)
- Existing test suite still passes: python -m pytest tests/ -x
</verification>

<success_criteria>
1. generate_comparison.py is a standalone, reusable comparison script
2. COMPARISON.md contains complete v3.0 vs v2.0 comparison with verdict, tables, prose, and chart references
3. Charts visualize per-timestep and scalar metric comparisons clearly
4. Tradeoffs documented neutrally per user decision
5. User approves the final comparison report
</success_criteria>

<output>
After completion, create `.planning/phases/11-integration-testing-validation/11-02-SUMMARY.md`
</output>
