---
phase: 11-integration-testing-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.yaml
  - outputs/test_results.json
  - outputs/training_history.json
  - outputs/training_history.png
  - outputs/predictions.png
autonomous: false
requirements: []

must_haves:
  truths:
    - "All 206 existing unit tests pass before training begins"
    - "A 3-epoch smoke test completes without NaN losses, crashes, or errors"
    - "Smoke test shows loss decreasing and temporal variation ratio > 0"
    - "A full 50-epoch training run completes without errors or NaN losses with all v3.0 features enabled"
    - "Test set evaluation produces outputs/test_results.json with all 16 metrics"
  artifacts:
    - path: "outputs/test_results.json"
      provides: "All v3.0 test metrics for comparison"
      contains: "temporal_variation_ratio"
    - path: "outputs/training_history.json"
      provides: "Per-epoch training metrics for v3.0 run"
      contains: "train_loss"
    - path: "outputs/training_history.png"
      provides: "Training curves visualization"
    - path: "outputs/predictions.png"
      provides: "Sample prediction visualizations"
  key_links:
    - from: "config.yaml"
      to: "main.py"
      via: "run_training reads config"
      pattern: "load_config.*config.yaml"
    - from: "main.py"
      to: "outputs/test_results.json"
      via: "json.dump after validate()"
      pattern: "test_results\\.json"
---

<objective>
Run the complete v3.0 training pipeline end-to-end with all features enabled (SA-ConvLSTM, temporal attention, attention gates, temporal loss, cosine LR, flare oversampling, balanced augmentation) and verify integration.

Purpose: Validate that all Phase 7-10 features work together without crashes, NaN, or errors when trained on real data for a full 50-epoch run. This produces the v3.0 results that Plan 02 will compare against the v2.0 baseline.

Output: outputs/test_results.json (v3.0 metrics), outputs/training_history.json, outputs/predictions.png
</objective>

<execution_context>
@/Users/indra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/indra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-integration-testing-validation/11-CONTEXT.md
@.planning/phases/11-integration-testing-validation/11-RESEARCH.md
@config.yaml
@main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight checks and smoke test</name>
  <files>config.yaml</files>
  <action>
Step 1 - Run the full existing test suite to confirm no regressions before training:
```
python -m pytest tests/ -x --timeout=60
```
All 206 tests (or current count) must pass, 1 skipped (MPS marker) is OK.

Step 2 - Verify config.yaml has the expected v3.0 defaults. Confirm these values are set:
- model.channels: [32, 64, 128]
- model.kernel_size: 5
- model.dropout_rate: 0.15
- model.use_sa_convlstm: true
- model.temporal_attention: true
- model.attention_gate: true
- model.delta_scale_init: 100.0
- training.epochs: 50
- training.tf_start: 0.0
- training.scheduler.type: "cosine"
- loss.temporal_diff_weight: 1.0
- loss.temporal_var_lambda: 0.1
- loss.asymmetric_weight: 0.5
- data.augmentation: "balanced"
- data.flare_oversample_weight: 3.0
- seed: 42
If any differ, fix config.yaml to match v3.0 defaults.

Step 3 - Run a 3-epoch smoke test. Temporarily change training.epochs to 3 in config.yaml, then run:
```
python main.py
```
Verify:
- No NaN/Inf in any loss value
- Training loss is decreasing (epoch 3 loss < epoch 1 loss)
- No Python errors or crashes
- Temporal variation ratio > 0 in test output
- Attention entropy reported (not collapsed to near-zero)
- outputs/test_results.json is created and contains all expected keys

If smoke test fails:
- NaN loss: Reduce temporal_var_lambda from 0.1 to 0.05, or temporal_diff_weight from 1.0 to 0.5
- OOM: Enable use_checkpointing: true
- Attention collapse (entropy less than 0.5): Note as diagnostic finding, do not block
- Other errors: Diagnose and fix the root cause

Step 4 - After smoke test passes, restore training.epochs to 50 in config.yaml.
  </action>
  <verify>
    <automated>python -m pytest tests/ -x --timeout=60</automated>
  </verify>
  <done>All existing tests pass. Config.yaml verified correct for v3.0. 3-epoch smoke test completed without NaN, crashes, or errors. Loss is decreasing. Config restored to epochs: 50.</done>
</task>

<task type="auto">
  <name>Task 2: Full 50-epoch training run</name>
  <files>outputs/test_results.json, outputs/training_history.json, outputs/training_history.png, outputs/predictions.png</files>
  <action>
Run the full 50-epoch v3.0 training with all features enabled:
```
python main.py
```

This will execute the entire pipeline:
1. Load data with balanced augmentation and flare oversampling (3x)
2. Create SA-ConvLSTM model with temporal attention, attention gates, delta scale
3. Train for 50 epochs with cosine LR scheduler (eta_min=1e-6), no teacher forcing
4. Evaluate best model on test set with all 16+ metrics
5. Save test_results.json, training_history.json, training_history.png, predictions.png

Monitor during training for:
- NaN losses (max_consecutive_nan: 10 will auto-abort)
- Gradient norm warnings (threshold: 100.0)
- Temporal attention entropy (should stay above 0.5)
- Delta scale value tracking

Expected runtime: 2-8 hours on MPS (Mac GPU).

If issues arise during training:
- NaN loss: Reduce temporal_var_lambda to 0.05 in config.yaml and re-run
- Training diverges: Reduce lr from 0.0001 to 0.00005 and re-run
- OOM: Set use_checkpointing: true in config.yaml and re-run
- Attention entropy collapse: Document as finding, do not adjust mid-run

After training completes, verify outputs exist:
- outputs/test_results.json (all metrics present)
- outputs/training_history.json (50 epochs of data)
- outputs/training_history.png (training curves)
- outputs/predictions.png (sample predictions)
- outputs/checkpoints/best_model.pt (best checkpoint)
  </action>
  <verify>
    <automated>python -c "import json; r=json.load(open('outputs/test_results.json')); assert 'temporal_variation_ratio' in r; assert 'test_csi' in r; assert len(r['test_mae_per_timestep'])==4; print('All 16 metrics present'); print(f'Var ratio: {r[\"temporal_variation_ratio\"]:.4f}'); print(f'CSI: {r[\"test_csi\"]:.4f}')"</automated>
  </verify>
  <done>Full 50-epoch training completed. outputs/test_results.json contains all 16 metrics. outputs/training_history.json contains 50 epochs of data. Training curves and prediction visualizations saved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify training results</name>
  <files>outputs/test_results.json, outputs/training_history.png, outputs/predictions.png</files>
  <action>Human verifies the v3.0 training run completed successfully and results look reasonable before proceeding to comparison report generation.</action>
  <verify>
    <automated>python -c "import json; r=json.load(open('outputs/test_results.json')); print(f'Var ratio: {r[\"temporal_variation_ratio\"]:.4f} (v2.0: 0.060)'); print(f'CSI: {r[\"test_csi\"]:.4f} (v2.0: 0.051)')"</automated>
  </verify>
  <done>User reviewed training output, loss curves, and predictions. Confirmed results are reasonable and approved proceeding to comparison report.</done>
  <what-built>Complete v3.0 training run with all features (SA-ConvLSTM, temporal attention, attention gates, temporal loss, cosine LR, flare oversampling). The model trained for 50 epochs and produced full evaluation metrics on the test set.</what-built>
  <how-to-verify>
    1. Check the training console output -- did training complete without NaN or errors?
    2. Review outputs/training_history.png -- are loss curves converging? Is validation loss stable?
    3. Review outputs/predictions.png -- do predicted frames show temporal variation (not flat/identical)?
    4. Quick metric check: run `python -c "import json; r=json.load(open('outputs/test_results.json')); print(f'Var ratio: {r[\"temporal_variation_ratio\"]:.4f} (v2.0: 0.060)'); print(f'CSI: {r[\"test_csi\"]:.4f} (v2.0: 0.051)')"`
    5. Is there anything unexpected that should be documented before proceeding to comparison report?
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to comparison report generation, or describe any issues to investigate.</resume-signal>
</task>

</tasks>

<verification>
- All 206+ existing tests pass (no regressions)
- 3-epoch smoke test completes without errors
- Full 50-epoch training completes without NaN or crashes
- outputs/test_results.json contains all 16 metric fields
- outputs/training_history.json contains 50 epochs of training data
- Training curves show convergence (loss decreasing or stable)
</verification>

<success_criteria>
1. Existing test suite passes with no regressions
2. Full v3.0 training run completes without errors, crashes, or NaN losses
3. Test evaluation metrics saved to outputs/test_results.json with temporal_variation_ratio, test_csi, test_hss, and all per-timestep metrics
4. Training visualizations (history, predictions) generated successfully
</success_criteria>

<output>
After completion, create `.planning/phases/11-integration-testing-validation/11-01-SUMMARY.md`
</output>
